
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#000000">
<title>IPNOTEC · Free Internet</title>
<link rel="preconnect" href="https://fonts.googleapis.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
<noscript><link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet"></noscript>
<style>
/* ═══════════════════════════════════════
   RESET & ROOT
═══════════════════════════════════════ */
:root {
  --bg:    #000000;
  --panel: #050505;
  --b1:    #0a0a0a;
  --b2:    #111111;
  --b3:    #1a1a1a;
  --dim:   #0d2a0d;
  --g:     #39FF14;
  --v:     #BF00FF;
  --i:     #6A0EFF;
  --b:     #00C8FF;
  --y:     #FFE600;
  --o:     #FF6200;
  --u:     #FF00FF;
  --r:     #FF2244;
  --w:     #FFFFFF;
  --gr:    #AAAAAA;
  --blk:   #222222;
  --font:  'Space Mono', monospace;
  --head:  'Orbitron', sans-serif;
}
*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; -webkit-tap-highlight-color:transparent; }
html, body { width:100%; height:100%; background:#000; overflow:hidden; }
body {
  font-family: var(--font);
  color: #fff;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  position: relative;
}

/* scanlines */
body::after {
  content:''; position:fixed; inset:0; pointer-events:none; z-index:9999;
  background: repeating-linear-gradient(0deg, transparent, transparent 3px, rgba(0,0,0,0.08) 3px, rgba(0,0,0,0.08) 4px);
}

/* ═══════════════════════════════════════
   PHONE SHELL
═══════════════════════════════════════ */
.shell {
  width: 100vw;
  height: 100vh;
  max-width: 480px;
  background: #000;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  position: relative;
}

/* ── STATUS BAR ── */
.status-bar {
  height: 48px;
  background: #000;
  border-bottom: 1px solid #0a1a0a;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 20px;
  flex-shrink: 0;
  z-index: 100;
}
.sb-brand {
  font-family: var(--head);
  font-size: 0.78rem;
  letter-spacing: 0.22em;
  color: var(--g);
  text-shadow: 0 0 10px var(--g);
}
.sb-right {
  display: flex;
  align-items: center;
  gap: 10px;
}
.sb-live {
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 0.6rem;
  color: var(--g);
  letter-spacing: 0.1em;
}
.sb-dot {
  width: 6px; height: 6px; border-radius: 50%;
  background: var(--g);
  box-shadow: 0 0 6px var(--g);
  animation: blink 1.8s ease-in-out infinite;
}
.sb-clock {
  font-size: 0.62rem;
  color: #2a4a2a;
  letter-spacing: 0.1em;
}
@keyframes blink { 0%,100%{opacity:1;} 50%{opacity:0.25;} }

/* ── SCREENS ── */
.screen {
  position: absolute;
  inset: 48px 0 0;
  display: flex;
  flex-direction: column;
  opacity: 0;
  pointer-events: none;
  transform: translateX(24px);
  transition: opacity 0.35s ease, transform 0.35s cubic-bezier(0.4,0,0.2,1);
  overflow-y: auto;
  overflow-x: hidden;
}
.screen.active {
  opacity: 1;
  pointer-events: all;
  transform: translateX(0);
}
.screen.out {
  opacity: 0;
  transform: translateX(-24px);
}

/* ═══════════════════════════════════════
   SCREEN 1 — CONNECT
═══════════════════════════════════════ */
#sc-connect {
  background: #000;
  padding: 0;
  justify-content: space-between;
}
.connect-hero {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 32px 28px 20px;
  gap: 0;
}
.free-badge {
  font-size: 0.6rem;
  letter-spacing: 0.25em;
  color: var(--dim);
  margin-bottom: 24px;
  text-transform: uppercase;
}
/* ASCII wifi */
.ascii-wifi {
  font-size: 1.05rem;
  line-height: 2.1;
  color: var(--g);
  text-shadow: 0 0 8px var(--g);
  text-align: center;
  margin-bottom: 20px;
  letter-spacing: 0.08em;
}
.aw-l1 { opacity: 0.12; animation: awpulse 2.4s ease-in-out infinite 0s; }
.aw-l2 { opacity: 0.35; animation: awpulse 2.4s ease-in-out infinite 0.3s; }
.aw-l3 { opacity: 0.7;  animation: awpulse 2.4s ease-in-out infinite 0.6s; }
.aw-l4 { opacity: 1; }
@keyframes awpulse { 0%,100%{text-shadow:0 0 6px var(--g);} 50%{text-shadow:0 0 18px var(--g),0 0 40px var(--g);} }

.ssid-name {
  font-family: var(--head);
  font-size: 1.1rem;
  font-weight: 700;
  letter-spacing: 0.22em;
  color: var(--g);
  text-shadow: 0 0 14px var(--g);
  margin-bottom: 6px;
  text-align: center;
}
.ssid-sub {
  font-size: 0.62rem;
  letter-spacing: 0.18em;
  color: #1a3a1a;
  text-align: center;
  margin-bottom: 32px;
}

/* Input */
.inp-label {
  display: block;
  font-size: 0.58rem;
  letter-spacing: 0.2em;
  color: #1a3a1a;
  margin-bottom: 8px;
  text-transform: uppercase;
}
.inp-wrap {
  width: 100%;
  border: 1px solid #0d2a0d;
  background: rgba(57,255,20,0.02);
  display: flex;
  align-items: stretch;
  margin-bottom: 8px;
  transition: border-color 0.2s, box-shadow 0.2s;
}
.inp-wrap:focus-within {
  border-color: var(--g);
  box-shadow: 0 0 0 3px rgba(57,255,20,0.08);
}
.inp-prefix {
  padding: 0 14px;
  font-size: 0.78rem;
  letter-spacing: 0.08em;
  color: #2a4a2a;
  border-right: 1px solid #0d1f0d;
  display: flex;
  align-items: center;
  background: rgba(57,255,20,0.02);
  flex-shrink: 0;
  white-space: nowrap;
}
.phone-inp {
  flex: 1;
  background: transparent;
  border: none;
  outline: none;
  color: var(--g);
  font-family: var(--font);
  font-size: 1.05rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  padding: 16px 14px;
}
.phone-inp::placeholder { color: #0d2a0d; font-weight: 400; font-size: 0.78rem; letter-spacing: 0.08em; }
.inp-hint { font-size: 0.55rem; color: #0d1f0d; text-align: center; margin-bottom: 24px; letter-spacing: 0.12em; }

.btn-connect {
  width: 100%;
  padding: 18px;
  background: transparent;
  border: 1px solid var(--g);
  color: var(--g);
  font-family: var(--head);
  font-size: 0.82rem;
  font-weight: 700;
  letter-spacing: 0.22em;
  cursor: pointer;
  text-shadow: 0 0 8px var(--g);
  box-shadow: 0 0 16px rgba(57,255,20,0.1);
  transition: all 0.2s;
  text-transform: uppercase;
}
.btn-connect:hover, .btn-connect:active { background: rgba(57,255,20,0.07); }
.btn-connect:disabled { opacity: 0.3; cursor: not-allowed; }

.connect-footer {
  padding: 16px 28px 24px;
  flex-shrink: 0;
}
.legal-txt {
  font-size: 0.5rem;
  color: #0d1f0d;
  text-align: center;
  letter-spacing: 0.1em;
  line-height: 1.9;
}

/* ═══════════════════════════════════════
   SCREEN 2 — OTP
═══════════════════════════════════════ */
#sc-otp {
  background: #000;
  align-items: center;
  justify-content: center;
  padding: 28px;
  gap: 0;
}
.otp-step-lbl {
  font-size: 0.55rem;
  letter-spacing: 0.22em;
  color: #1a3a1a;
  text-align: center;
  margin-bottom: 10px;
}
.otp-title {
  font-family: var(--head);
  font-size: 1.15rem;
  font-weight: 700;
  letter-spacing: 0.15em;
  color: #fff;
  text-align: center;
  margin-bottom: 6px;
}
.otp-phone-show {
  font-size: 0.72rem;
  color: #3a5a3a;
  letter-spacing: 0.12em;
  text-align: center;
  margin-bottom: 24px;
}
/* progress bar */
.otp-bar-wrap {
  width: 100%;
  height: 2px;
  background: #0a0a0a;
  margin-bottom: 24px;
  overflow: hidden;
}
.otp-bar-fill {
  height: 100%;
  width: 0%;
  background: var(--g);
  box-shadow: 0 0 8px var(--g);
  transition: width 0.1s linear;
}
/* digits */
.otp-digits {
  display: flex;
  gap: 8px;
  justify-content: center;
  margin-bottom: 14px;
  cursor: text;
}
.od {
  width: 42px;
  height: 54px;
  background: rgba(57,255,20,0.02);
  border: 1px solid #0d2a0d;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--head);
  font-size: 1.3rem;
  font-weight: 900;
  color: var(--g);
  text-shadow: 0 0 12px var(--g);
  transition: all 0.25s;
  overflow: hidden;
}
.od.lit { border-color: var(--g); background: rgba(57,255,20,0.05); box-shadow: 0 0 12px rgba(57,255,20,0.2); }
.od.done { border-color: var(--g); background: rgba(57,255,20,0.08); }
@keyframes digitPop {
  0%   { transform: translateY(24px) scale(0.8); opacity: 0; }
  65%  { transform: translateY(-4px) scale(1.06); }
  100% { transform: translateY(0) scale(1); opacity: 1; }
}
.od-digit { display: inline-block; animation: digitPop 0.38s cubic-bezier(0.34,1.56,0.64,1) both; }

.otp-status {
  font-size: 0.6rem;
  letter-spacing: 0.18em;
  color: #1a4a1a;
  text-align: center;
  min-height: 20px;
  transition: color 0.3s;
}
.otp-status.verified { color: var(--g); text-shadow: 0 0 8px var(--g); }

/* node preview in otp */
.otp-nodes {
  margin-top: 24px;
  width: 100%;
  border-top: 1px solid #0a1a0a;
  padding-top: 16px;
}
.on-lbl { font-size: 0.5rem; letter-spacing: 0.2em; color: #0d1f0d; margin-bottom: 10px; text-transform: uppercase; }
.on-row {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 7px 0;
  border-bottom: 1px solid #080808;
}
.on-row:last-child { border-bottom: none; }
.on-sq { width: 10px; height: 10px; flex-shrink: 0; }
.on-id { font-size: 0.65rem; font-weight: 700; letter-spacing: 0.1em; flex: 1; }
.on-time { font-size: 0.55rem; color: #2a2a2a; letter-spacing: 0.08em; }

/* ═══════════════════════════════════════
   SCREEN 3 — CONNECTED: MAP + CHAT
═══════════════════════════════════════ */
#sc-connected {
  background: #000;
  display: flex;
  flex-direction: column;
}

/* top info bar */
.conn-infobar {
  background: #000;
  border-bottom: 1px solid #0a1a0a;
  padding: 10px 18px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-shrink: 0;
}
.ci-left { display: flex; align-items: center; gap: 8px; }
.ci-badge {
  display: flex; align-items: center; gap: 5px;
  border: 1px solid #0d2a0d;
  padding: 4px 10px;
  background: rgba(57,255,20,0.04);
}
.ci-badge-dot { width: 5px; height: 5px; border-radius: 50%; background: var(--g); box-shadow: 0 0 5px var(--g); animation: blink 1.6s infinite; }
.ci-badge-txt { font-size: 0.58rem; color: var(--g); letter-spacing: 0.14em; }
.ci-you-tag {
  font-size: 0.6rem;
  color: #2a4a2a;
  letter-spacing: 0.08em;
}
.ci-you-sq { display: inline-block; width: 9px; height: 9px; vertical-align: middle; margin-right: 4px; }
.ci-right { text-align: right; }
.ci-timer { font-family: var(--head); font-size: 0.72rem; color: var(--g); letter-spacing: 0.08em; }
.ci-timer.warn { color: var(--r); text-shadow: 0 0 8px var(--r); animation: blink 1s infinite; }
.ci-timer-lbl { font-size: 0.45rem; color: #1a2a1a; letter-spacing: 0.1em; margin-top: 2px; }

/* RADAR MAP */
.radar-section {
  position: relative;
  flex-shrink: 0;
  background: #000;
  overflow: hidden;
}
.radar-canvas { display: block; width: 100%; }
.radar-you-label {
  position: absolute;
  top: 10px; left: 12px;
  font-size: 0.55rem;
  letter-spacing: 0.12em;
  color: rgba(57,255,20,0.5);
  border: 1px solid #0d2a0d;
  padding: 3px 8px;
  background: rgba(0,0,0,0.7);
  pointer-events: none;
}
.radar-count {
  position: absolute;
  bottom: 10px; right: 12px;
  font-size: 0.55rem;
  color: #1a3a1a;
  letter-spacing: 0.1em;
  background: rgba(0,0,0,0.7);
  padding: 3px 8px;
  border: 1px solid #0a1a0a;
  pointer-events: none;
}

/* CHAT */
.chat-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  background: #000;
  border-top: 1px solid #0a1a0a;
  min-height: 0;
  transition: box-shadow 0.4s, border-color 0.4s;
}
.chat-section.ping-glow { border-top-color: var(--ping-color, var(--g)); box-shadow: 0 -4px 20px rgba(57,255,20,0.15); }

.chat-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 16px;
  border-bottom: 1px solid #080808;
  flex-shrink: 0;
}
.chat-head-title { font-size: 0.55rem; letter-spacing: 0.18em; color: #1a3a1a; text-transform: uppercase; }
.chat-head-right { display: flex; align-items: center; gap: 8px; }
.chat-nodes-live { font-size: 0.52rem; color: var(--g); letter-spacing: 0.1em; }

.chat-log {
  flex: 1;
  overflow-y: auto;
  padding: 10px 16px;
  display: flex;
  flex-direction: column;
  gap: 0;
  min-height: 120px;
  max-height: 220px;
}
.chat-msg {
  display: flex;
  align-items: baseline;
  gap: 8px;
  padding: 4px 0;
  border-bottom: 1px solid #060606;
  animation: msgIn 0.3s ease both;
}
@keyframes msgIn { from{opacity:0;transform:translateY(4px);} to{opacity:1;transform:translateY(0);} }
.cm-time { font-size: 0.5rem; color: #0d1f0d; flex-shrink: 0; letter-spacing: 0.04em; }
.cm-sq { width: 8px; height: 8px; flex-shrink: 0; align-self: center; }
.cm-id { font-size: 0.6rem; font-weight: 700; letter-spacing: 0.06em; flex-shrink: 0; }
.cm-arrow { font-size: 0.5rem; color: #0d1f0d; flex-shrink: 0; }
.cm-text { font-size: 0.65rem; color: #555; flex: 1; line-height: 1.5; letter-spacing: 0.03em; }
.cm-text.you { color: var(--g); }

/* ping response flash */
.chat-msg.ping-response { animation: pingFlash 0.6s ease both; }
@keyframes pingFlash { 0%{background:rgba(57,255,20,0.08);} 100%{background:transparent;} }

.chat-inp-row {
  display: flex;
  align-items: stretch;
  border-top: 1px solid #0a1a0a;
  flex-shrink: 0;
}
.chat-you-sq { width: 8px; height: 8px; flex-shrink: 0; align-self: center; margin-left: 14px; }
.chat-inp {
  flex: 1;
  background: transparent;
  border: none;
  outline: none;
  color: var(--g);
  font-family: var(--font);
  font-size: 0.72rem;
  padding: 14px 12px;
  letter-spacing: 0.06em;
}
.chat-inp::placeholder { color: #0d1f0d; font-size: 0.62rem; }
.chat-send {
  padding: 14px 18px;
  background: transparent;
  border: none;
  border-left: 1px solid #0a1a0a;
  color: var(--g);
  font-family: var(--head);
  font-size: 0.6rem;
  letter-spacing: 0.14em;
  cursor: pointer;
  transition: all 0.15s;
  text-transform: uppercase;
  flex-shrink: 0;
}
.chat-send:hover { background: rgba(57,255,20,0.06); }
.chat-send:active { background: rgba(57,255,20,0.12); }

/* ═══════════════════════════════════════
   SCREEN 4 — SESSION REPORT
═══════════════════════════════════════ */
#sc-report {
  background: #000;
  overflow-y: auto;
  padding: 0;
}
.rep-header {
  background: #000;
  border-bottom: 1px solid #0a1a0a;
  padding: 20px 20px 16px;
  text-align: center;
}
.rep-disconnected {
  display: inline-block;
  font-size: 0.55rem;
  letter-spacing: 0.2em;
  color: var(--r);
  border: 1px solid rgba(255,34,68,0.25);
  padding: 3px 10px;
  margin-bottom: 10px;
  background: rgba(255,34,68,0.04);
}
.rep-title {
  font-family: var(--head);
  font-size: 1.1rem;
  font-weight: 900;
  letter-spacing: 0.2em;
  color: var(--g);
  text-shadow: 0 0 14px var(--g);
  line-height: 1.2;
}
.rep-body { padding: 16px 20px 32px; }
.rep-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 0;
  border-bottom: 1px solid #080808;
}
.rep-row:last-of-type { border-bottom: none; }
.rep-k { font-size: 0.58rem; color: #1a3a1a; letter-spacing: 0.14em; text-transform: uppercase; }
.rep-v { font-size: 0.68rem; color: #fff; letter-spacing: 0.08em; font-weight: 700; }
.rep-v.green { color: var(--g); }
.rep-divider { height: 1px; background: #0a1a0a; margin: 16px 0; }
/* bar chart */
.rep-chart-lbl { font-size: 0.52rem; letter-spacing: 0.18em; color: #1a3a1a; margin-bottom: 10px; text-transform: uppercase; }
.rep-bars { display: flex; align-items: flex-end; gap: 4px; height: 52px; }
.rep-bar { flex: 1; background: #080808; position: relative; overflow: hidden; }
.rep-bar-fill { position: absolute; bottom: 0; left: 0; right: 0; background: var(--g); box-shadow: 0 0 6px var(--g); }
.rep-ticks { display: flex; gap: 4px; margin-top: 4px; }
.rep-tick { flex: 1; font-size: 0.42rem; color: #0d1f0d; text-align: center; letter-spacing: 0.04em; }
/* ping list */
.ping-section { margin-top: 20px; }
.ping-sec-lbl { font-size: 0.52rem; letter-spacing: 0.18em; color: #1a3a1a; margin-bottom: 10px; text-transform: uppercase; }
.ping-item {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 0; border-bottom: 1px solid #080808;
}
.ping-item:last-child { border-bottom: none; }
.pi-sq { width: 10px; height: 10px; flex-shrink: 0; }
.pi-name { font-size: 0.65rem; letter-spacing: 0.08em; flex: 1; }
.pi-count { font-size: 0.58rem; color: #2a4a2a; letter-spacing: 0.06em; }
/* reconnect */
.rep-reconnect {
  margin-top: 20px;
  border: 1px solid #0d2a0d;
  padding: 14px;
  text-align: center;
  font-size: 0.58rem;
  color: #0d2a0d;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.2s;
}
.rep-reconnect:hover { border-color: var(--g); color: var(--g); background: rgba(57,255,20,0.04); }

/* ═══════════════════════════════════════
   ADMIN TRIGGER (floating)
═══════════════════════════════════════ */
.admin-trigger {
  position: fixed;
  bottom: 20px; right: 20px;
  z-index: 800;
  width: 44px; height: 44px;
  border: 1px solid #1a1a1a;
  background: #050505;
  color: #222;
  font-family: var(--font);
  font-size: 0.55rem;
  letter-spacing: 0.1em;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s;
}
.admin-trigger:hover { border-color: #333; color: #555; }
.admin-trigger span { font-size: 0.9rem; }

/* ═══════════════════════════════════════
   ADMIN OVERLAY
═══════════════════════════════════════ */
.admin-overlay {
  position: fixed; inset: 0; z-index: 900;
  background: rgba(0,0,0,0.96);
  display: none;
  flex-direction: column;
  overflow: hidden;
}
.admin-overlay.open { display: flex; }

/* Admin header */
.admin-topbar {
  height: 52px;
  background: #000;
  border-bottom: 1px solid #1a1a1a;
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 18px;
  flex-shrink: 0;
}
.at-left { display: flex; align-items: center; gap: 10px; }
.at-badge {
  background: var(--r);
  font-family: var(--head);
  font-size: 0.48rem;
  letter-spacing: 0.16em;
  padding: 4px 9px;
  color: #fff;
  display: flex; align-items: center; gap: 4px;
}
.at-badge-dot { width: 5px; height: 5px; border-radius: 50%; background: #fff; animation: blink 1.2s infinite; }
.at-title { font-family: var(--head); font-size: 0.82rem; font-weight: 900; letter-spacing: 0.2em; color: #fff; }
.at-title span { color: var(--g); text-shadow: 0 0 10px var(--g); }
.at-close {
  background: transparent; border: 1px solid #1a1a1a;
  color: #444; font-family: var(--font); font-size: 0.6rem;
  padding: 6px 12px; cursor: pointer; letter-spacing: 0.1em;
  transition: all 0.2s;
}
.at-close:hover { border-color: var(--r); color: var(--r); }

/* Admin stats */
.admin-stats {
  display: grid; grid-template-columns: repeat(2,1fr); gap: 1px;
  background: #111; flex-shrink: 0;
  border-bottom: 1px solid #111;
}
.a-stat { background: #000; padding: 14px 16px; }
.a-stat-lbl { font-size: 0.48rem; letter-spacing: 0.18em; color: #333; text-transform: uppercase; margin-bottom: 5px; }
.a-stat-val { font-family: var(--head); font-size: 1.05rem; font-weight: 700; color: #fff; }
.a-stat-val.g { color: var(--g); text-shadow: 0 0 8px var(--g); }
.a-stat-val.o { color: var(--o); }
.a-stat-val.b { color: var(--b); }
.a-stat-sub { font-size: 0.42rem; color: #2a2a2a; margin-top: 3px; letter-spacing: 0.08em; }

/* Admin scroll body */
.admin-body { flex: 1; overflow-y: auto; padding: 0; }

/* Expandable section */
.exp { background: #000; border-bottom: 1px solid #111; }
.exp-hdr {
  display: flex; align-items: center; justify-content: space-between;
  padding: 16px 18px; cursor: pointer; user-select: none;
  transition: background 0.15s;
  border-bottom: 1px solid transparent;
}
.exp-hdr:hover { background: #050505; }
.exp-hdr.open { border-bottom-color: #111; }
.exp-hdr-left { display: flex; align-items: center; gap: 10px; }
.exp-arrow {
  font-size: 0.65rem; color: #2a2a2a;
  transition: transform 0.25s, color 0.2s;
  width: 14px; flex-shrink: 0;
}
.exp-hdr.open .exp-arrow { transform: rotate(90deg); color: var(--g); }
.exp-title { font-family: var(--head); font-size: 0.72rem; font-weight: 700; letter-spacing: 0.15em; color: #fff; }
.exp-title-num { color: var(--g); }
.exp-right { display: flex; align-items: center; gap: 8px; }
.exp-meta { font-size: 0.48rem; color: #333; letter-spacing: 0.1em; }
.exp-tag {
  font-size: 0.45rem; letter-spacing: 0.1em;
  padding: 3px 8px; border: 1px solid #1a1a1a; color: #444;
}
.exp-tag.live { border-color: rgba(57,255,20,0.25); color: var(--g); }
.exp-tag.warn { border-color: rgba(255,98,0,0.25); color: var(--o); }
.exp-body { max-height: 0; overflow: hidden; transition: max-height 0.4s cubic-bezier(0.4,0,0.2,1); }
.exp-body.open { max-height: 800px; }
.exp-inner { padding: 14px 18px 18px; }

/* Table */
.a-tbl { width: 100%; border-collapse: collapse; }
.a-tbl th {
  font-size: 0.45rem; letter-spacing: 0.18em; color: #2a2a2a;
  padding: 6px 8px; border-bottom: 1px solid #111;
  text-align: left; font-weight: 400; text-transform: uppercase;
}
.a-tbl td { padding: 10px 8px; border-bottom: 1px solid #080808; font-size: 0.58rem; color: #666; vertical-align: middle; }
.a-tbl tr:hover td { background: #030303; color: #888; }
.a-tbl td.w { color: #fff; font-weight: 700; }
.a-tbl td.g { color: var(--g); }
.a-tbl td.o { color: var(--o); }
.tsq { display: inline-block; width: 9px; height: 9px; vertical-align: middle; margin-right: 5px; }
.prog-track { height: 3px; background: #111; display: inline-block; width: 50px; vertical-align: middle; }
.prog-fill { height: 100%; }
.ta-btn {
  padding: 4px 9px; background: transparent; border: 1px solid #1a1a1a;
  color: #444; font-family: var(--font); font-size: 0.42rem; letter-spacing: 0.1em;
  cursor: pointer; transition: all 0.15s; text-transform: uppercase;
}
.ta-btn:hover { border-color: #fff; color: #fff; }
.ta-btn.red:hover { border-color: var(--r); color: var(--r); }
.ta-btn.grn:hover { border-color: var(--g); color: var(--g); }

/* Chat monitor */
.chat-mon { height: 180px; overflow-y: auto; background: #030303; border: 1px solid #0a0a0a; padding: 8px 12px; }
.cm-ln { font-size: 0.58rem; letter-spacing: 0.04em; padding: 3px 0; border-bottom: 1px solid #060606; line-height: 1.7; display: flex; align-items: baseline; gap: 6px; }
.cm-ln-time { color: #1a1a1a; flex-shrink: 0; }
.cm-ln-who { font-weight: 700; flex-shrink: 0; }
.cm-ln-arrow { color: #111; flex-shrink: 0; }
.cm-ln-msg { color: #444; }

/* usage bars */
.ub-row { display: grid; grid-template-columns: 70px 1fr 44px; align-items: center; gap: 8px; margin-bottom: 9px; }
.ub-who { font-size: 0.55rem; display: flex; align-items: center; gap: 5px; letter-spacing: 0.05em; }
.ub-track { height: 6px; background: #0a0a0a; position: relative; overflow: hidden; }
.ub-fill { height: 100%; position: absolute; left: 0; top: 0; opacity: 0.75; }
.ub-val { font-size: 0.5rem; color: #444; text-align: right; }

/* toggles */
.tog-row { display: flex; align-items: center; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #080808; }
.tog-row:last-child { border-bottom: none; }
.tog-name { font-size: 0.6rem; color: #888; letter-spacing: 0.08em; }
.tog { width: 32px; height: 16px; background: #111; border: 1px solid #1a1a1a; position: relative; cursor: pointer; flex-shrink: 0; }
.tog-knob { position: absolute; top: 3px; left: 3px; width: 8px; height: 8px; background: #333; transition: all 0.2s; }
.tog.on .tog-knob { left: 19px; background: var(--g); box-shadow: 0 0 5px var(--g); }
.tog.on { border-color: #0d2a0d; }

/* archive */
.arc-tbl td { font-size: 0.55rem; }

/* scrollbar */
::-webkit-scrollbar { width: 3px; height: 3px; }
::-webkit-scrollbar-track { background: #000; }
::-webkit-scrollbar-thumb { background: #0d2a0d; }

/* pin overlay */
.pin-overlay {
  position: fixed; inset: 0; z-index: 950;
  background: rgba(0,0,0,0.98);
  display: none; align-items: center; justify-content: center;
  flex-direction: column; gap: 20px;
}
.pin-overlay.open { display: flex; }
.pin-title { font-family: var(--head); font-size: 0.82rem; color: #fff; letter-spacing: 0.2em; }
.pin-sub { font-size: 0.52rem; color: #1a3a1a; letter-spacing: 0.15em; }
.pin-dots { display: flex; gap: 10px; }
.pin-dot { width: 14px; height: 14px; border: 1px solid #0d2a0d; background: transparent; transition: all 0.2s; }
.pin-dot.filled { background: var(--g); border-color: var(--g); box-shadow: 0 0 8px var(--g); }
.pin-pad { display: grid; grid-template-columns: repeat(3,1fr); gap: 8px; width: 200px; }
.pin-key {
  padding: 16px; background: transparent; border: 1px solid #0d2a0d;
  color: var(--g); font-family: var(--head); font-size: 0.9rem; font-weight: 700;
  cursor: pointer; letter-spacing: 0.08em; transition: all 0.15s; text-align: center;
}
.pin-key:hover { background: rgba(57,255,20,0.07); }
.pin-key:active { background: rgba(57,255,20,0.14); }
.pin-err { font-size: 0.52rem; color: var(--r); letter-spacing: 0.15em; min-height: 18px; }
.pin-cancel { font-size: 0.52rem; color: #1a1a1a; letter-spacing: 0.15em; cursor: pointer; padding: 8px; transition: color 0.2s; }
.pin-cancel:hover { color: #444; }

/* ── misc util ── */
.sep { height: 1px; background: #0a1a0a; }

/* ── FORM EXTRAS ── */
.form-error {
  font-size: 0.55rem;
  color: var(--r);
  letter-spacing: 0.12em;
  text-align: center;
  min-height: 18px;
  margin-top: 8px;
}
.otp-hidden-inp {
  position: absolute;
  left: -9999px;
  opacity: 0;
  width: 1px;
  height: 1px;
}
.btn-verify {
  width: 100%;
  padding: 16px;
  background: transparent;
  border: 1px solid var(--g);
  color: var(--g);
  font-family: var(--head);
  font-size: 0.78rem;
  font-weight: 700;
  letter-spacing: 0.2em;
  cursor: pointer;
  text-shadow: 0 0 8px var(--g);
  box-shadow: 0 0 12px rgba(57,255,20,0.1);
  transition: all 0.2s;
  text-transform: uppercase;
  margin-top: 18px;
}
.btn-verify:hover { background: rgba(57,255,20,0.07); }
.btn-verify:disabled { opacity: 0.3; cursor: not-allowed; }
.otp-resend {
  font-size: 0.52rem;
  color: #1a3a1a;
  letter-spacing: 0.12em;
  margin-top: 12px;
  cursor: pointer;
  transition: color 0.2s;
}
.otp-resend:hover { color: var(--g); }
.otp-resend.disabled { pointer-events: none; opacity: 0.3; }

/* ── WELCOME SCREEN ── */
#sc-welcome {
  background: #000;
  align-items: center;
  justify-content: center;
  padding: 28px;
}
.terms-box {
  width: 100%;
  border: 1px solid #0d2a0d;
  background: rgba(57,255,20,0.02);
  padding: 20px;
  margin-bottom: 24px;
  max-height: 240px;
  overflow-y: auto;
}
.terms-text {
  font-size: 0.58rem;
  color: #3a5a3a;
  line-height: 2;
  letter-spacing: 0.06em;
}
.terms-text strong {
  color: var(--g);
  font-weight: 700;
}

/* ── ADMIN LOGIN ── */
.admin-login-overlay {
  position: fixed; inset: 0; z-index: 950;
  background: rgba(0,0,0,0.98);
  display: none; align-items: center; justify-content: center;
  flex-direction: column; gap: 20px;
}
.admin-login-overlay.open { display: flex; }
.admin-login-form {
  display: flex; flex-direction: column; gap: 12px; width: 260px;
}
.admin-inp {
  background: transparent;
  border: 1px solid #0d2a0d;
  color: var(--g);
  font-family: var(--font);
  font-size: 0.78rem;
  padding: 14px;
  letter-spacing: 0.08em;
  outline: none;
  transition: border-color 0.2s;
}
.admin-inp:focus { border-color: var(--g); }
.admin-inp::placeholder { color: #0d2a0d; font-size: 0.65rem; }
</style>
</head>
<body>

<div class="shell" id="shell">

  <!-- ── STATUS BAR ── -->
  <div class="status-bar">
    <div class="sb-brand">IPNOTEC</div>
    <div class="sb-right">
      <div class="sb-live"><div class="sb-dot"></div>FREE INTERNET</div>
      <div class="sb-clock" id="sb-clock">--:--</div>
    </div>
  </div>

  <!-- ════════════════════
       SCREEN 1: CONNECT
  ════════════════════ -->
  <div class="screen active" id="sc-connect">
    <div class="connect-hero">
      <div class="free-badge">◈ FREE WIFI ACCESS · VERIFIED</div>
      <div class="ascii-wifi">
        <div class="aw-l1">(( &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; ))</div>
        <div class="aw-l2">&nbsp;&nbsp;(( &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; ))</div>
        <div class="aw-l3">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;(( &nbsp; ))</div>
        <div class="aw-l4">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;●</div>
      </div>
      <div class="ssid-name">IPNOTEC_HUB</div>
      <div class="ssid-sub">FREE · OTP VERIFIED · BENGALURU</div>
      <label class="inp-label" for="name-field">YOUR NAME</label>
      <div class="inp-wrap">
        <input class="phone-inp" id="name-field" type="text" inputmode="text" maxlength="100"
          placeholder="FULL NAME" style="font-size:0.85rem;font-weight:400;"
          oninput="checkConnectForm()">
      </div>
      <label class="inp-label" for="email-field">EMAIL ADDRESS</label>
      <div class="inp-wrap">
        <input class="phone-inp" id="email-field" type="email" inputmode="email"
          placeholder="EMAIL" style="font-size:0.85rem;font-weight:400;"
          oninput="checkConnectForm()">
      </div>
      <label class="inp-label" for="phone-field">MOBILE NUMBER</label>
      <div class="inp-wrap">
        <div class="inp-prefix">🇮🇳 +91</div>
        <input class="phone-inp" id="phone-field" type="tel" inputmode="numeric"
          maxlength="10" placeholder="XXXXXXXXXX"
          oninput="this.value=this.value.replace(/\D/g,''); checkConnectForm()">
      </div>
      <div class="inp-hint">A 6-DIGIT OTP WILL BE SENT TO YOUR EMAIL</div>
      <div class="form-error" id="connect-error"></div>
    </div>
    <div class="connect-footer">
      <button class="btn-connect" id="btn-connect" onclick="doConnect()" disabled>
        SEND OTP →
      </button>
      <div class="legal-txt" style="margin-top:14px;">
        BY CONNECTING YOU AGREE TO IPNOTEC TERMS OF SERVICE<br>
        FREE · 24 HOUR SESSION · AUTO DISCONNECT<br>
        IPNOTEC CORP · CIN U63122KA2026OPC214615
      </div>
    </div>
  </div>

  <!-- ════════════════════
       SCREEN 2: OTP
  ════════════════════ -->
  <div class="screen" id="sc-otp">
    <div style="padding:36px 28px; display:flex; flex-direction:column; align-items:center; flex:1;">
      <div style="width:100%;margin-bottom:16px;"><button class="back-btn" onclick="goScreen('sc-connect')" style="background:transparent;border:1px solid #0d2a0d;color:#2a4a2a;font-family:var(--font);font-size:0.55rem;letter-spacing:0.12em;padding:6px 14px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.borderColor='var(--g)';this.style.color='var(--g)'" onmouseout="this.style.borderColor='#0d2a0d';this.style.color='#2a4a2a'">← BACK</button></div>
      <div class="otp-step-lbl">STEP 02 · AUTHENTICATE</div>
      <div class="otp-title">VERIFYING YOUR EMAIL</div>
      <div class="otp-phone-show" id="otp-num-show">check your inbox</div>
      <div class="otp-bar-wrap"><div class="otp-bar-fill" id="otp-bar"></div></div>
      <div class="otp-digits" onclick="document.getElementById('otp-input').focus()">
        <div class="od" id="od0"></div>
        <div class="od" id="od1"></div>
        <div class="od" id="od2"></div>
        <div class="od" id="od3"></div>
        <div class="od" id="od4"></div>
        <div class="od" id="od5"></div>
      </div>
      <input type="tel" id="otp-input" class="otp-hidden-inp" maxlength="6"
        inputmode="numeric" oninput="onOtpInput(this.value)">
      <div class="otp-status" id="otp-status">ENTER 6-DIGIT OTP</div>
      <button class="btn-verify" id="btn-verify" onclick="verifyOTP()" disabled>
        VERIFY OTP →
      </button>
      <div class="form-error" id="otp-error"></div>
      <div class="otp-resend disabled" id="otp-resend" onclick="resendOTP()">RESEND OTP</div>
      <div class="otp-nodes" id="otp-nodes">
        <div class="on-lbl">NODES CURRENTLY ON THIS NETWORK</div>
        <!-- populated by JS -->
      </div>
    </div>
  </div>

  <!-- ════════════════════
       SCREEN 3: WELCOME
  ════════════════════ -->
  <div class="screen" id="sc-welcome">
    <div style="padding:48px 28px; display:flex; flex-direction:column; align-items:center; flex:1; justify-content:center;">
      <div style="width:100%;margin-bottom:16px;"><button class="back-btn" onclick="goScreen('sc-otp')" style="background:transparent;border:1px solid #0d2a0d;color:#2a4a2a;font-family:var(--font);font-size:0.55rem;letter-spacing:0.12em;padding:6px 14px;cursor:pointer;transition:all 0.2s;" onmouseover="this.style.borderColor='var(--g)';this.style.color='var(--g)'" onmouseout="this.style.borderColor='#0d2a0d';this.style.color='#2a4a2a'">← BACK</button></div>
      <div class="otp-step-lbl">STEP 03 · TERMS & CONDITIONS</div>
      <div class="otp-title" style="margin-bottom:6px;">WELCOME TO IPNOTEC</div>
      <div class="otp-phone-show" style="margin-bottom:20px;">FREE COWORKING INTERNET · BENGALURU</div>
      <div class="terms-box">
        <div class="terms-text">
          <strong>TERMS OF USE — IPNOTEC FREE WIFI</strong><br><br>
          1. This is a <strong>free internet service</strong> provided by IPNOTEC for coworking members and visitors.<br><br>
          2. Each session lasts <strong>24 hours</strong> from the time of connection.<br><br>
          3. By using this service, you agree that your <strong>name, email, and phone number</strong> will be stored for identity verification and security purposes.<br><br>
          4. You agree not to use this network for any <strong>illegal, harmful, or unauthorized activities</strong>.<br><br>
          5. IPNOTEC reserves the right to <strong>revoke access</strong> at any time without prior notice.<br><br>
          6. Network speed and availability are <strong>not guaranteed</strong>. Use at your own risk.<br><br>
          <strong>IPNOTEC CORP · CIN U63122KA2026OPC214615</strong>
        </div>
      </div>
      <button class="btn-connect" id="btn-accept" onclick="doAccept()">
        ACCEPT & CONNECT →
      </button>
      <div class="form-error" id="accept-error"></div>
    </div>
  </div>

  <!-- ════════════════════
       SCREEN 4: CONNECTED
  ════════════════════ -->
  <div class="screen" id="sc-connected">
    <div class="conn-infobar">
      <div class="ci-left">
        <div class="ci-badge"><div class="ci-badge-dot"></div><div class="ci-badge-txt">CONNECTED</div></div>
        <div class="ci-you-tag" id="ci-you-tag">
          <span class="ci-you-sq" id="ci-you-sq"></span><span id="ci-you-name">GREEN</span>
        </div>
      </div>
      <div class="ci-right">
        <div class="ci-timer" id="ci-timer">24:00:00</div>
        <div class="ci-timer-lbl">SESSION REMAINING</div>
      </div>
    </div>

    <div class="radar-section" id="radar-section">
      <canvas class="radar-canvas" id="radar-canvas"></canvas>
      <div class="radar-you-label" id="radar-you-label">YOU ARE ■ GREEN</div>
      <div class="radar-count" id="radar-count">7 NODES LIVE</div>
    </div>

    <div class="chat-section" id="chat-section">
      <div class="chat-head">
        <div class="chat-head-title">◈ NODE CHAT · ANONYMOUS</div>
        <div class="chat-head-right">
          <div class="chat-nodes-live" id="chat-nodes-live">● 7 LIVE</div>
        </div>
      </div>
      <div class="chat-log" id="chat-log"></div>
      <div class="chat-inp-row">
        <div class="chat-you-sq" id="chat-you-sq" style="background:var(--g);"></div>
        <input class="chat-inp" id="chat-inp" placeholder="TYPE MESSAGE AND PING →"
          onkeydown="if(event.key==='Enter')sendChat()">
        <button class="chat-send" onclick="sendChat()">PING</button>
      </div>
    </div>
  </div>

  <!-- ════════════════════
       SCREEN 4: REPORT
  ════════════════════ -->
  <div class="screen" id="sc-report">
    <div class="rep-header">
      <div class="rep-disconnected">● AUTO DISCONNECTED · 24HR LIMIT REACHED</div>
      <div class="rep-title">SESSION<br>REPORT</div>
    </div>
    <div class="rep-body">
      <div class="rep-row"><div class="rep-k">Phone</div><div class="rep-v" id="rep-phone">—</div></div>
      <div class="rep-row"><div class="rep-k">Color Node</div><div class="rep-v" id="rep-color">—</div></div>
      <div class="rep-row"><div class="rep-k">Connected At</div><div class="rep-v" id="rep-conntime">—</div></div>
      <div class="rep-row"><div class="rep-k">Disconnected</div><div class="rep-v" id="rep-disctime">—</div></div>
      <div class="rep-row"><div class="rep-k">Duration</div><div class="rep-v">24:00:00</div></div>
      <div class="rep-row"><div class="rep-k">Data Used</div><div class="rep-v" id="rep-data">—</div></div>
      <div class="rep-row"><div class="rep-k">Nodes Pinged</div><div class="rep-v green" id="rep-pinged">—</div></div>
      <div class="rep-row"><div class="rep-k">Messages Sent</div><div class="rep-v" id="rep-msgs">—</div></div>
      <div class="rep-divider"></div>
      <div class="rep-chart-lbl">HOURLY DATA USAGE</div>
      <div class="rep-bars" id="rep-bars"></div>
      <div class="rep-ticks" id="rep-ticks"></div>
      <div class="ping-section">
        <div class="ping-sec-lbl">NODES PINGED THIS SESSION</div>
        <div id="rep-ping-list"></div>
      </div>
      <div class="rep-reconnect" onclick="doReconnect()">
        ↺ &nbsp; RECONNECT TO IPNOTEC FREE INTERNET
      </div>
    </div>
  </div>

</div><!-- /shell -->

<script>
// ══════════════════════════════════════════════
// WEB AUDIO ENGINE
// ══════════════════════════════════════════════
let audioCtx = null;

function getCtx() {
  if (!audioCtx) audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  return audioCtx;
}

function playTone(freq, dur, type='sine', vol=0.18, attack=0.01, decay=0.1) {
  try {
    const ctx = getCtx();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.type = type;
    osc.frequency.setValueAtTime(freq, ctx.currentTime);
    gain.gain.setValueAtTime(0, ctx.currentTime);
    gain.gain.linearRampToValueAtTime(vol, ctx.currentTime + attack);
    gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + dur);
    osc.start(ctx.currentTime);
    osc.stop(ctx.currentTime + dur + 0.05);
  } catch(e){}
}

function playRise() {
  // connect button click: rising two-tone
  playTone(440, 0.12, 'sine', 0.12);
  setTimeout(() => playTone(660, 0.14, 'sine', 0.12), 80);
}

function playOTPDigit(idx) {
  // each digit: ascending pitches 300→420→540→660
  const freqs = [320, 400, 520, 660];
  playTone(freqs[idx], 0.1, 'square', 0.08);
}

function playVerified() {
  // success chord arpeggio
  [523, 659, 784, 1047].forEach((f, i) => {
    setTimeout(() => playTone(f, 0.18, 'sine', 0.13), i * 70);
  });
}

function playConnected() {
  // warm resonant connected tone
  playTone(220, 0.5, 'sine', 0.12);
  setTimeout(() => playTone(330, 0.4, 'sine', 0.08), 100);
  setTimeout(() => playTone(440, 0.35, 'sine', 0.06), 200);
}

function playChatReceive() {
  // subtle incoming message tick
  playTone(880, 0.07, 'sine', 0.07);
}

function playPingResponse() {
  // brighter ping response alert — two quick rising tones
  playTone(660, 0.1, 'sine', 0.14);
  setTimeout(() => playTone(990, 0.15, 'sine', 0.14), 90);
  setTimeout(() => playTone(1320, 0.2, 'sine', 0.1), 180);
}

function playDisconnect() {
  // descending goodbye tone
  [440, 330, 220, 165].forEach((f, i) => {
    setTimeout(() => playTone(f, 0.22, 'sine', 0.1), i * 100);
  });
}

function playAdminOpen() {
  playTone(1200, 0.08, 'square', 0.06);
  setTimeout(() => playTone(800, 0.08, 'square', 0.06), 60);
}

// ══════════════════════════════════════════════
// STATE
// ══════════════════════════════════════════════
const VIBGYOR = [
  { key:'V', name:'VIOLET',  hex:'#BF00FF' },
  { key:'I', name:'INDIGO',  hex:'#6A0EFF' },
  { key:'B', name:'BLUE',    hex:'#00C8FF' },
  { key:'G', name:'GREEN',   hex:'#39FF14' },
  { key:'Y', name:'YELLOW',  hex:'#FFE600' },
  { key:'O', name:'ORANGE',  hex:'#FF6200' },
  { key:'U', name:'UV',      hex:'#FF00FF' },
  { key:'W', name:'WHITE',   hex:'#FFFFFF' },
  { key:'GR',name:'GREY',    hex:'#AAAAAA' },
];

const PORTAL = window.__PORTAL__ || { clientMac:'', apMac:'', ssid:'', radioId:0, redirectUrl:'' };

let S = {
  phone: '',
  name: '',
  email: '',
  otpToken: null,
  authToken: null,
  color: null,
  connectedAt: null,
  sessionSecs: 24 * 3600,
  sessionInterval: null,
  chatMessages: [],
  msgCount: 0,
  pingCount: 0,
  dataUsed: 0,
  hourlyData: [],
  pinnedNodes: {},
  lastPingTime: 0,
  awaitingResponse: false,
};

// Simulated nodes on the network
let networkNodes = [
  { key:'V', name:'VIOLET',  hex:'#BF00FF', connTime:'06:28', timeLeft:17*3600+32*60, data:512, msgs:14, x:0, y:0 },
  { key:'I', name:'INDIGO',  hex:'#6A0EFF', connTime:'08:14', timeLeft:15*3600+46*60, data:288, msgs:8,  x:0, y:0 },
  { key:'B', name:'BLUE',    hex:'#00C8FF', connTime:'09:01', timeLeft:14*3600+59*60, data:122, msgs:6,  x:0, y:0 },
  { key:'Y', name:'YELLOW',  hex:'#FFE600', connTime:'07:45', timeLeft:2*3600+15*60,  data:1100,msgs:22, x:0, y:0 },
  { key:'O', name:'ORANGE',  hex:'#FF6200', connTime:'10:05', timeLeft:13*3600+55*60, data:44,  msgs:3,  x:0, y:0 },
  { key:'W', name:'WHITE',   hex:'#FFFFFF', connTime:'10:22', timeLeft:13*3600+38*60, data:38,  msgs:5,  x:0, y:0 },
];

let allChatMessages = []; // for admin monitor
let archiveSessions = [];
let pinEntry = '';

// ══════════════════════════════════════════════
// CLOCK
// ══════════════════════════════════════════════
function updateClock() {
  const d = new Date();
  const h = String(d.getHours()).padStart(2,'0');
  const m = String(d.getMinutes()).padStart(2,'0');
  const el = document.getElementById('sb-clock');
  if (el) el.textContent = h + ':' + m;
  const a = document.getElementById('adm-clock');
  const s = String(d.getSeconds()).padStart(2,'0');
  if (a) a.textContent = h + ':' + m + ':' + s;
}
setInterval(updateClock, 1000);
updateClock();

// ══════════════════════════════════════════════
// SCREEN TRANSITIONS
// ══════════════════════════════════════════════
let currentScreen = 'sc-connect';
function goScreen(id) {
  // Dismiss keyboard before transitioning
  if (document.activeElement) document.activeElement.blur();
  const from = document.getElementById(currentScreen);
  const to   = document.getElementById(id);
  from.classList.add('out');
  setTimeout(() => {
    from.classList.remove('active', 'out');
    to.classList.add('active');
    currentScreen = id;
  }, 340);
}

// ══════════════════════════════════════════════
// API HELPERS
// ══════════════════════════════════════════════
async function apiPost(path, body, token) {
  const headers = { 'Content-Type': 'application/json' };
  if (token) headers['Authorization'] = 'Bearer ' + token;
  const res = await fetch(path, { method: 'POST', headers, body: JSON.stringify(body) });
  return res.json();
}

// ══════════════════════════════════════════════
// CONNECT FLOW
// ══════════════════════════════════════════════
function checkConnectForm() {
  const name = document.getElementById('name-field').value.trim();
  const email = document.getElementById('email-field').value.trim();
  const phone = document.getElementById('phone-field').value;
  const valid = name.length > 0 && email.includes('@') && email.includes('.') && phone.length === 10;
  document.getElementById('btn-connect').disabled = !valid;
}

async function doConnect() {
  const name = document.getElementById('name-field').value.trim();
  const email = document.getElementById('email-field').value.trim();
  const phone = document.getElementById('phone-field').value;
  const errEl = document.getElementById('connect-error');
  const btn = document.getElementById('btn-connect');

  btn.disabled = true;
  btn.textContent = 'SENDING OTP…';
  errEl.textContent = '';

  try {
    const data = await apiPost('/portal/send-otp', {
      name, email, phone: '91' + phone,
      clientMac: PORTAL.clientMac,
      apMac: PORTAL.apMac,
      ssid: PORTAL.ssid,
      radioId: PORTAL.radioId,
      redirectUrl: PORTAL.redirectUrl,
    });

    if (!data.success) {
      errEl.textContent = data.message || 'Failed to send OTP';
      btn.disabled = false;
      btn.textContent = 'SEND OTP →';
      return;
    }

    S.phone = phone;
    S.name = name;
    S.email = email;
    S.otpToken = data.otpToken;
    playRise();

    // Populate OTP screen
    document.getElementById('otp-num-show').textContent = email;
    const nodesEl = document.getElementById('otp-nodes');
    let html = '<div class="on-lbl">NODES CURRENTLY ON THIS NETWORK</div>';
    networkNodes.forEach(n => {
      html += `<div class="on-row">
        <div class="on-sq" style="background:${n.hex};box-shadow:0 0 6px ${n.hex};"></div>
        <div class="on-id" style="color:${n.hex};">${n.name}</div>
        <div class="on-time">ACTIVE · ${n.connTime}</div>
      </div>`;
    });
    nodesEl.innerHTML = html;

    // Reset OTP UI
    for (let i = 0; i < 6; i++) {
      const cell = document.getElementById('od' + i);
      cell.innerHTML = ''; cell.className = 'od';
    }
    document.getElementById('otp-input').value = '';
    document.getElementById('btn-verify').disabled = true;
    document.getElementById('otp-error').textContent = '';
    document.getElementById('otp-status').textContent = 'ENTER 6-DIGIT OTP';
    document.getElementById('otp-status').classList.remove('verified');
    document.getElementById('otp-bar').style.width = '0%';

    goScreen('sc-otp');
    startOtpTimer();
    setTimeout(() => document.getElementById('otp-input').focus(), 400);
  } catch(e) {
    errEl.textContent = 'Network error. Please try again.';
    btn.disabled = false;
    btn.textContent = 'SEND OTP →';
  }
}

function formatHM(t) {
  return t;
}

// ══════════════════════════════════════════════
// OTP ENTRY & VERIFICATION
// ══════════════════════════════════════════════
function onOtpInput(val) {
  val = val.replace(/\D/g, '').slice(0, 6);
  document.getElementById('otp-input').value = val;
  for (let i = 0; i < 6; i++) {
    const cell = document.getElementById('od' + i);
    if (i < val.length) {
      cell.innerHTML = `<span class="od-digit">${val[i]}</span>`;
      cell.classList.add('lit');
      cell.classList.remove('done');
    } else {
      cell.innerHTML = '';
      cell.classList.remove('lit', 'done');
    }
  }
  document.getElementById('btn-verify').disabled = val.length !== 6;
  if (val.length === 6) playOTPDigit(3);
}

let otpTimerInterval = null;

function startOtpTimer() {
  const resendEl = document.getElementById('otp-resend');
  resendEl.classList.add('disabled');
  let secs = 30;
  resendEl.textContent = `RESEND OTP (${secs}s)`;
  clearInterval(otpTimerInterval);
  otpTimerInterval = setInterval(() => {
    secs--;
    if (secs <= 0) {
      clearInterval(otpTimerInterval);
      resendEl.textContent = 'RESEND OTP';
      resendEl.classList.remove('disabled');
    } else {
      resendEl.textContent = `RESEND OTP (${secs}s)`;
    }
  }, 1000);
}

async function resendOTP() {
  const errEl = document.getElementById('otp-error');
  errEl.textContent = '';
  try {
    const data = await apiPost('/portal/send-otp', {
      name: S.name, email: S.email, phone: '91' + S.phone,
      clientMac: PORTAL.clientMac,
      apMac: PORTAL.apMac,
      ssid: PORTAL.ssid,
      radioId: PORTAL.radioId,
      redirectUrl: PORTAL.redirectUrl,
    });
    if (data.success) {
      S.otpToken = data.otpToken;
      document.getElementById('otp-status').textContent = 'NEW OTP SENT TO EMAIL';
      startOtpTimer();
      document.getElementById('otp-input').value = '';
      onOtpInput('');
    } else {
      errEl.textContent = data.message || 'Failed to resend OTP';
    }
  } catch(e) {
    errEl.textContent = 'Network error';
  }
}

async function verifyOTP() {
  const otp = document.getElementById('otp-input').value;
  if (otp.length !== 6) return;
  const errEl = document.getElementById('otp-error');
  const btn = document.getElementById('btn-verify');
  btn.disabled = true;
  btn.textContent = 'VERIFYING…';
  errEl.textContent = '';

  try {
    const data = await apiPost('/portal/verify-otp', {
      otp, otpToken: S.otpToken,
    });

    if (!data.success) {
      errEl.textContent = data.message || 'Invalid OTP';
      btn.disabled = false;
      btn.textContent = 'VERIFY OTP →';
      return;
    }

    S.authToken = data.authToken;

    // Show verified state
    for (let i = 0; i < 6; i++) {
      document.getElementById('od' + i).classList.remove('lit');
      document.getElementById('od' + i).classList.add('done');
    }
    playVerified();
    const st = document.getElementById('otp-status');
    st.textContent = '✓ IDENTITY VERIFIED';
    st.classList.add('verified');
    clearInterval(otpTimerInterval);
    document.getElementById('otp-bar').style.width = '100%';

    setTimeout(() => goScreen('sc-welcome'), 800);
  } catch(e) {
    errEl.textContent = 'Network error. Please try again.';
    btn.disabled = false;
    btn.textContent = 'VERIFY OTP →';
  }
}

async function doAccept() {
  const errEl = document.getElementById('accept-error');
  const btn = document.getElementById('btn-accept');
  btn.disabled = true;
  btn.textContent = 'CONNECTING…';
  errEl.textContent = '';

  try {
    const data = await apiPost('/portal/accept', {}, S.authToken);

    if (!data.success) {
      errEl.textContent = data.message || 'Connection failed';
      btn.disabled = false;
      btn.textContent = 'ACCEPT & CONNECT →';
      return;
    }

    // Assign color for the demo UI
    const taken = networkNodes.map(n => n.key);
    const avail = VIBGYOR.filter(v => !taken.includes(v.key));
    S.color = avail[Math.floor(Math.random() * avail.length)] || VIBGYOR[3];

    playConnected();
    setupConnectedScreen();
    goScreen('sc-connected');
    startSession();
  } catch(e) {
    errEl.textContent = 'Network error. Please try again.';
    btn.disabled = false;
    btn.textContent = 'ACCEPT & CONNECT →';
  }
}

// ══════════════════════════════════════════════
// CONNECTED SCREEN SETUP
// ══════════════════════════════════════════════
function setupConnectedScreen() {
  const c = S.color;
  // Update color tags
  const youTag = document.getElementById('ci-you-tag');
  const sq = document.getElementById('ci-you-sq');
  sq.style.background = c.hex;
  sq.style.boxShadow = `0 0 6px ${c.hex}`;
  document.getElementById('ci-you-name').textContent = c.name;
  document.getElementById('radar-you-label').innerHTML = `YOU ARE <span style="color:${c.hex};text-shadow:0 0 6px ${c.hex};">■ ${c.name}</span>`;
  document.getElementById('chat-you-sq').style.background = c.hex;

  // Build radar
  initRadar();
  // Seed chat
  seedChat();
  // Start data simulation
  S.dataUsed = Math.floor(Math.random() * 10 + 2);
  S.hourlyData = Array.from({length:8}, () => 0);
  setInterval(() => {
    S.dataUsed += Math.random() * 8 + 1;
    const slot = Math.min(Math.floor((24*3600 - S.sessionSecs) / (3*3600)), 7);
    S.hourlyData[slot] = (S.hourlyData[slot] || 0) + Math.random() * 12;
  }, 8000);

  // Simulate nodes joining/leaving every 30-60s
  simulateLiveNodes();
}

// ══════════════════════════════════════════════
// RADAR CANVAS
// ══════════════════════════════════════════════
let radarAnimFrame = null;
let radarDots = [];
let sweepAngle = 0;

function initRadar() {
  const section = document.getElementById('radar-section');
  const canvas  = document.getElementById('radar-canvas');
  const W = section.clientWidth;
  const H = Math.min(W * 0.65, 260);
  canvas.width  = W;
  canvas.height = H;
  section.style.height = H + 'px';

  const cx = W / 2, cy = H / 2;
  const maxR = Math.min(W, H) * 0.44;

  // Place nodes radially
  const allNodes = [...networkNodes];
  radarDots = allNodes.map((n, idx) => {
    const angle = (idx / allNodes.length) * Math.PI * 2 + Math.random() * 0.4 - 0.2;
    const dist  = maxR * (0.45 + Math.random() * 0.5);
    return {
      x: cx + Math.cos(angle) * dist,
      y: cy + Math.sin(angle) * dist,
      color: n.hex,
      key: n.key,
      name: n.name,
      pulse: Math.random() * Math.PI * 2,
      isYou: false,
    };
  });
  // Add YOU
  const youAngle = Math.random() * Math.PI * 2;
  const youDist  = maxR * (0.35 + Math.random() * 0.35);
  radarDots.push({
    x: cx + Math.cos(youAngle) * youDist,
    y: cy + Math.sin(youAngle) * youDist,
    color: S.color.hex,
    key: S.color.key,
    name: S.color.name,
    pulse: 0,
    isYou: true,
  });

  cancelAnimationFrame(radarAnimFrame);
  drawRadar(canvas, cx, cy, maxR, W, H);
}

function drawRadar(canvas, cx, cy, maxR, W, H) {
  const ctx = canvas.getContext('2d');

  function frame() {
    ctx.clearRect(0, 0, W, H);

    // Grid
    ctx.strokeStyle = '#050f05';
    ctx.lineWidth = 1;
    for (let x = 0; x < W; x += 22) {
      ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,H); ctx.stroke();
    }
    for (let y = 0; y < H; y += 22) {
      ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(W,y); ctx.stroke();
    }

    // Rings
    [0.25, 0.5, 0.75, 1.0].forEach(r => {
      ctx.beginPath();
      ctx.arc(cx, cy, maxR * r, 0, Math.PI * 2);
      ctx.strokeStyle = '#060f06';
      ctx.lineWidth = 1;
      ctx.stroke();
    });

    // Sweep
    sweepAngle += 0.018;
    const sweepGrad = ctx.createConicalGradient ? null : null;
    ctx.save();
    ctx.translate(cx, cy);
    ctx.rotate(sweepAngle);
    const sg = ctx.createLinearGradient(0, 0, maxR, 0);
    sg.addColorStop(0, 'rgba(57,255,20,0.0)');
    sg.addColorStop(0.7, 'rgba(57,255,20,0.03)');
    sg.addColorStop(1, 'rgba(57,255,20,0.07)');
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.arc(0, 0, maxR, -0.45, 0.45);
    ctx.closePath();
    ctx.fillStyle = sg;
    ctx.fill();
    ctx.restore();

    // Connection lines YOU → HUB and others → HUB
    radarDots.forEach(dot => {
      ctx.beginPath();
      ctx.moveTo(cx, cy);
      ctx.lineTo(dot.x, dot.y);
      if (dot.isYou) {
        ctx.strokeStyle = `rgba(${hexToRgb(dot.color)},0.4)`;
        ctx.setLineDash([4, 6]);
        ctx.lineWidth = 1.2;
      } else {
        ctx.strokeStyle = 'rgba(255,255,255,0.05)';
        ctx.setLineDash([3, 8]);
        ctx.lineWidth = 0.8;
      }
      ctx.stroke();
      ctx.setLineDash([]);
    });

    // Hub
    const hubSize = 16;
    ctx.fillStyle = '#39FF14';
    ctx.shadowColor = '#39FF14';
    ctx.shadowBlur = 14;
    ctx.fillRect(cx - hubSize/2, cy - hubSize/2, hubSize, hubSize);
    ctx.shadowBlur = 0;
    ctx.fillStyle = '#000';
    ctx.font = 'bold 6px Orbitron, monospace';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    ctx.fillText('HUB', cx, cy);

    // VIBGYOR square dots
    radarDots.forEach(dot => {
      dot.pulse += 0.04;
      const alpha = dot.isYou ? 1 : (0.7 + Math.sin(dot.pulse) * 0.3);
      const sz = dot.isYou ? 13 : 10;
      const rgb = hexToRgb(dot.color);

      if (dot.isYou) {
        ctx.shadowColor = dot.color;
        ctx.shadowBlur = 12 + Math.sin(dot.pulse) * 6;
      }
      ctx.globalAlpha = alpha;
      ctx.fillStyle = dot.color;
      ctx.fillRect(dot.x - sz/2, dot.y - sz/2, sz, sz);
      ctx.globalAlpha = 1;
      ctx.shadowBlur = 0;

      // label
      ctx.font = dot.isYou ? 'bold 7px Space Mono, monospace' : '6px Space Mono, monospace';
      ctx.textAlign = 'center';
      ctx.textBaseline = 'top';
      ctx.fillStyle = dot.color;
      ctx.globalAlpha = dot.isYou ? 1 : 0.6;
      ctx.fillText(dot.isYou ? 'YOU' : dot.key, dot.x, dot.y + sz/2 + 3);
      ctx.globalAlpha = 1;
    });

    radarAnimFrame = requestAnimationFrame(frame);
  }
  frame();

  // Update count
  document.getElementById('radar-count').textContent = radarDots.length + ' NODES LIVE';
  document.getElementById('chat-nodes-live').textContent = '● ' + radarDots.length + ' LIVE';
}

function hexToRgb(hex) {
  const r = parseInt(hex.slice(1,3),16);
  const g = parseInt(hex.slice(3,5),16);
  const b = parseInt(hex.slice(5,7),16);
  return `${r},${g},${b}`;
}

// ══════════════════════════════════════════════
// CHAT
// ══════════════════════════════════════════════
const SEED_MSGS = [
  { node:'V', text:'anyone working on a pitch deck rn?' },
  { node:'I', text:'just dropped an investor email. fingers crossed 🤞' },
  { node:'B', text:'slide 12 is killing me. any designers here?' },
  { node:'Y', text:'charger anyone? 2nd floor near the window' },
  { node:'O', text:'need a co-founder? lol. SaaS infra background' },
  { node:'W', text:'who else is on the ground floor?' },
];

function seedChat() {
  const log = document.getElementById('chat-log');
  log.innerHTML = '';
  SEED_MSGS.forEach((m, i) => {
    const node = networkNodes.find(n => n.key === m.node);
    if (!node) return;
    setTimeout(() => {
      addChatMessage(node.key, node.name, node.hex, m.text, false);
    }, i * 220);
  });
}

function addChatMessage(key, name, hex, text, isYou, isResponse = false) {
  const log = document.getElementById('chat-log');
  const now = new Date();
  const t = String(now.getHours()).padStart(2,'0') + ':' + String(now.getMinutes()).padStart(2,'0');
  const div = document.createElement('div');
  div.className = 'chat-msg' + (isResponse ? ' ping-response' : '');
  div.innerHTML = `
    <span class="cm-time">[${t}]</span>
    <span class="cm-sq" style="background:${hex};${isYou?'box-shadow:0 0 5px '+hex+';':''}"></span>
    <span class="cm-id" style="color:${hex};">${isYou ? name+'-YOU' : name}</span>
    <span class="cm-arrow">>>></span>
    <span class="cm-text ${isYou ? 'you' : ''}">${text}</span>
  `;
  log.appendChild(div);
  log.scrollTop = log.scrollHeight;

  // Admin mirror
  const al = document.getElementById('adm-chat-log');
  const ad = document.createElement('div');
  ad.className = 'cm-ln';
  ad.innerHTML = `<span class="cm-ln-time">[${t}]</span><span class="cm-ln-who" style="color:${hex};">${name}</span><span class="cm-ln-arrow">>>></span><span class="cm-ln-msg">${text}</span>`;
  al.appendChild(ad);
  al.scrollTop = al.scrollHeight;

  allChatMessages.push({ t, key, name, hex, text, isYou });
  const meta = document.getElementById('exp-chat-meta');
  if (meta) meta.textContent = allChatMessages.length + ' MESSAGES';

  if (!isYou && !isResponse) playChatReceive();
}

function sendChat() {
  const inp = document.getElementById('chat-inp');
  const text = inp.value.trim();
  if (!text) return;
  inp.value = '';
  S.msgCount++;
  S.lastPingTime = Date.now();
  S.awaitingResponse = true;
  addChatMessage(S.color.key, S.color.name, S.color.hex, text, true);
  playRise();
  // Trigger response from random node after 2–5 seconds
  const delay = 2000 + Math.random() * 3000;
  setTimeout(() => {
    if (!S.awaitingResponse) return;
    S.awaitingResponse = false;
    const responder = networkNodes[Math.floor(Math.random() * networkNodes.length)];
    const responses = [
      'interesting, tell me more',
      'same here actually 🙌',
      'nice one',
      'lol true',
      'heard. heading out in 10',
      'totally agree',
      'anyone else feel this place has great energy?',
      'yep, same boat',
      'let\'s connect after the session',
      '++',
    ];
    const resp = responses[Math.floor(Math.random() * responses.length)];
    // Track ping
    if (!S.pinnedNodes[responder.key]) S.pinnedNodes[responder.key] = 0;
    S.pinnedNodes[responder.key]++;
    S.pingCount++;
    // Glow chat section
    const cs = document.getElementById('chat-section');
    cs.style.borderTopColor = responder.hex;
    cs.style.boxShadow = `0 -4px 24px rgba(${hexToRgb(responder.hex)},0.18)`;
    document.getElementById('chat-inp').style.caretColor = responder.hex;
    setTimeout(() => {
      cs.style.borderTopColor = '';
      cs.style.boxShadow = '';
    }, 2200);
    playPingResponse();
    addChatMessage(responder.key, responder.name, responder.hex, resp, false, true);
  }, delay);
}

// Periodic auto chat from nodes
function autoChatLoop() {
  const auto = [
    { node:'V', texts:['anyone got a good coffee order here?','still on that deck. send help','ok slide 12 fixed. FINALLY'] },
    { node:'B', texts:['just had a great call 🎉','networking in a co-working hits different','shoutout to whoever brought espresso'] },
    { node:'I', texts:['building in public starting today','hot take: async > meetings','any backend folks here?'] },
    { node:'Y', texts:['2 hrs left on my session','great focus energy today','BLR startup scene is 🔥'] },
    { node:'O', texts:['anyone else on fintech?','need a designer for a landing page','just pushed a new build'] },
    { node:'W', texts:['quiet day. perfect for deep work','loving this space','heads down, no calls 🙏'] },
  ];
  function send() {
    const pick = auto[Math.floor(Math.random() * auto.length)];
    const node = networkNodes.find(n => n.key === pick.node);
    if (node) {
      const text = pick.texts[Math.floor(Math.random() * pick.texts.length)];
      addChatMessage(node.key, node.name, node.hex, text, false);
    }
    setTimeout(send, 12000 + Math.random() * 18000);
  }
  setTimeout(send, 8000);
}

// ══════════════════════════════════════════════
// SESSION TIMER
// ══════════════════════════════════════════════
function startSession() {
  S.connectedAt = new Date();
  S.sessionSecs = 24 * 3600; // demo: 24hr
  autoChatLoop();
  S.sessionInterval = setInterval(() => {
    S.sessionSecs--;
    S.dataUsed += 0.002 + Math.random() * 0.004; // trickle
    const timerEl = document.getElementById('ci-timer');
    if (!timerEl) return;
    const h = Math.floor(S.sessionSecs / 3600);
    const m = String(Math.floor((S.sessionSecs % 3600) / 60)).padStart(2,'0');
    const s = String(S.sessionSecs % 60).padStart(2,'0');
    timerEl.textContent = `${h}:${m}:${s}`;
    if (S.sessionSecs <= 2 * 3600) timerEl.classList.add('warn');
    if (S.sessionSecs <= 0) endSession();
    // For demo: show report after 120s
    if (S.sessionSecs === (24 * 3600 - 120)) {
      // uncomment to auto-test report:
      // endSession();
    }
  }, 1000);
}

function endSession() {
  clearInterval(S.sessionInterval);
  playDisconnect();
  buildReport();
  setTimeout(() => goScreen('sc-report'), 600);
  // archive
  const disc = new Date();
  archiveSessions.unshift({
    color: S.color,
    phone: S.phone,
    connectedAt: S.connectedAt,
    disc,
    data: S.dataUsed.toFixed(0) + ' MB',
    pings: S.pingCount,
    end: 'AUTO',
  });

}

function buildReport() {
  document.getElementById('rep-phone').textContent = '+91 ' + S.phone.slice(0,6) + '••••';
  const cn = S.color;
  document.getElementById('rep-color').innerHTML = `<span style="display:inline-block;width:10px;height:10px;background:${cn.hex};vertical-align:middle;margin-right:5px;box-shadow:0 0 6px ${cn.hex};"></span><span style="color:${cn.hex};">■ ${cn.name}</span>`;
  const ct = S.connectedAt;
  document.getElementById('rep-conntime').textContent = ct ? ct.toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'}) : '--:--';
  document.getElementById('rep-disctime').textContent = new Date().toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'});
  document.getElementById('rep-data').textContent = S.dataUsed < 1024 ? Math.round(S.dataUsed) + ' MB' : (S.dataUsed/1024).toFixed(2) + ' GB';
  document.getElementById('rep-pinged').textContent = Object.keys(S.pinnedNodes).length;
  document.getElementById('rep-msgs').textContent = S.msgCount;

  // Bar chart
  const bars = document.getElementById('rep-bars');
  const ticks = document.getElementById('rep-ticks');
  const hours = ['10H','12H','14H','16H','18H','20H','22H','24H'];
  const heights = S.hourlyData.length ? S.hourlyData.map(v => {
    const max = Math.max(...S.hourlyData, 1);
    return Math.round((v / max) * 100);
  }) : [32,58,44,88,65,75,50,28];
  bars.innerHTML = hours.map((_,i) => `<div class="rep-bar"><div class="rep-bar-fill" style="height:${heights[i]||10}%;"></div></div>`).join('');
  ticks.innerHTML = hours.map(h => `<div class="rep-tick">${h}</div>`).join('');

  // Pinged nodes
  const pl = document.getElementById('rep-ping-list');
  const entries = Object.entries(S.pinnedNodes);
  if (entries.length === 0) {
    pl.innerHTML = '<div style="font-size:0.55rem;color:#1a1a1a;letter-spacing:0.12em;padding:8px 0;">NO NODES PINGED THIS SESSION</div>';
  } else {
    pl.innerHTML = entries.map(([k, count]) => {
      const node = VIBGYOR.find(v => v.key === k) || { name: k, hex: '#fff' };
      return `<div class="ping-item">
        <div class="pi-sq" style="background:${node.hex};box-shadow:0 0 5px ${node.hex};"></div>
        <div class="pi-name" style="color:${node.hex};">${node.name}</div>
        <div class="pi-count">${count} exchange${count>1?'s':''}</div>
      </div>`;
    }).join('');
  }
}

function doReconnect() {
  S.phone = ''; S.name = ''; S.email = '';
  S.otpToken = null; S.authToken = null;
  S.color = null; S.msgCount = 0; S.pingCount = 0;
  S.dataUsed = 0; S.hourlyData = []; S.pinnedNodes = {};
  S.sessionSecs = 24*3600;
  document.getElementById('name-field').value = '';
  document.getElementById('email-field').value = '';
  document.getElementById('phone-field').value = '';
  document.getElementById('btn-connect').disabled = true;
  document.getElementById('btn-connect').textContent = 'SEND OTP →';
  document.getElementById('connect-error').textContent = '';
  document.getElementById('chat-log').innerHTML = '';
  for (let i = 0; i < 6; i++) {
    const cell = document.getElementById('od' + i);
    cell.innerHTML = ''; cell.className = 'od';
  }
  document.getElementById('otp-input').value = '';
  document.getElementById('otp-status').textContent = 'ENTER 6-DIGIT OTP';
  document.getElementById('otp-status').classList.remove('verified');
  document.getElementById('otp-bar').style.width = '0%';
  document.getElementById('btn-verify').disabled = true;
  document.getElementById('btn-verify').textContent = 'VERIFY OTP →';
  document.getElementById('otp-error').textContent = '';
  document.getElementById('accept-error').textContent = '';
  document.getElementById('btn-accept').disabled = false;
  document.getElementById('btn-accept').textContent = 'ACCEPT & CONNECT →';
  clearInterval(otpTimerInterval);
  cancelAnimationFrame(radarAnimFrame);
  goScreen('sc-connect');
}

// ══════════════════════════════════════════════
// LIVE NODE SIMULATION
// ══════════════════════════════════════════════
function simulateLiveNodes() {
  function loop() {
    const delay = 20000 + Math.random() * 30000;
    setTimeout(() => {
      if (currentScreen !== 'sc-connected') { loop(); return; }
      const action = networkNodes.length < 3 ? 'join' : networkNodes.length >= 8 ? 'leave' : Math.random() > 0.45 ? 'join' : 'leave';
      if (action === 'leave') {
        const idx = Math.floor(Math.random() * networkNodes.length);
        networkNodes.splice(idx, 1);
        addChatMessage('SYS','SYSTEM','#333','[NODE LEFT THE NETWORK]',false);
      } else {
        const taken = [...networkNodes.map(n=>n.key), S.color.key];
        const avail = VIBGYOR.filter(v => !taken.includes(v.key));
        if (avail.length) {
          const n = avail[Math.floor(Math.random() * avail.length)];
          const newNode = { key:n.key, name:n.name, hex:n.hex, connTime:new Date().toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'}), timeLeft:24*3600, data:0, msgs:0, x:0, y:0 };
          networkNodes.push(newNode);
          addChatMessage(n.key, n.name, n.hex, 'just connected to IPNOTEC_HUB 👋', false);
        }
      }
      initRadar();
      document.getElementById('radar-count').textContent = (networkNodes.length+1) + ' NODES LIVE';
      document.getElementById('chat-nodes-live').textContent = '● ' + (networkNodes.length+1) + ' LIVE';

      loop();
    }, delay);
  }
  loop();
}

// ══════════════════════════════════════════════
// AUTO-REAUTH (returning client with active session)
// ══════════════════════════════════════════════
if (PORTAL.autoReauth) {
  const taken = networkNodes.map(n => n.key);
  const avail = VIBGYOR.filter(v => !taken.includes(v.key));
  S.color = avail[Math.floor(Math.random() * avail.length)] || VIBGYOR[3];
  playConnected();
  setupConnectedScreen();
  goScreen('sc-connected');
  startSession();
}
</script>

</body>
</html>